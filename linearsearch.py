def locate_card(cards,query):
    for i in range(n):
        if cards[i]==query:
            print("FOUND")
        else:
            print("FOUND")
n=int(input("Enter number of cards:"))
query=int(input("Enter number to be found:"))
cards=[]
for i in range(n):
    x=int(input("Enter numbers to be inserted:"))
    cards.append(x)
locate_card(cards,query)    
    



